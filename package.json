{
  "name": "vibe-oidc",
  "version": "0.1.0",
  "private": true,
  "description": "Local OIDC test provider boilerplate (Keycloak-compatible mindset)",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/cookie": "^11.0.2",
    "@fastify/formbody": "^8.0.2",
    "dotenv": "^16.4.5",
    "fastify": "^5.0.0",
    "jose": "^5.9.6",
    "oidc-provider": "^9.7.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
